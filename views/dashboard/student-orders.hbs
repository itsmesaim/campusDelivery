<div class="orders-page">
    <div class="page-header">
        <h1>My Orders</h1>
        <p>Track your order history and current deliveries</p>
        <a href="/dashboard/student" class="back-btn">← Back to Dashboard</a>
    </div>

    {{#if orders.length}}
    <div class="orders-list">
        {{#each orders}}
        <div class="order-card">
            <div class="order-header">
                <div class="order-info">
                    <h3>Order #{{this.orderNumber}}</h3>
                    <p class="vendor-name">{{this.vendorId.name}}</p>
                </div>
                <div class="order-status">
                    <span class="status-badge status-{{this.status}}">{{this.status}}</span>
                    <span class="order-total">₹{{this.total}}</span>
                </div>
            </div>

            <div class="order-details">
                <div class="order-items">
                    <h4>Items Ordered:</h4>
                    {{#each this.items}}
                    <div class="order-item">
                        <span class="item-name">{{this.name}} × {{this.quantity}}</span>
                        <span class="item-price">₹{{this.total}}</span>
                    </div>
                    {{/each}}
                </div>

                <div class="order-meta">
                    <p><strong>Ordered:</strong> {{formatDate this.createdAt}}</p>
                    <p><strong>Delivery Address:</strong> {{this.deliveryAddress}}</p>
                    <p><strong>Payment:</strong> {{this.paymentMethod}}</p>
                    {{#if this.notes}}
                    <p><strong>Notes:</strong> {{this.notes}}</p>
                    {{/if}}
                </div>
            </div>

            <div class="order-actions">
                <a href="/orders/track/{{this._id}}" class="track-btn">Track Order</a>
                {{#if (eq this.status 'DELIVERED')}}
                <button class="reorder-btn" onclick="reorderItems('{{this._id}}')">Reorder</button>
                {{/if}}
            </div>
        </div>
        {{/each}}
    </div>
    {{else}}
    <div class="empty-orders">
        <div class="empty-icon">📦</div>
        <h3>No orders yet</h3>
        <p>When you place orders, they'll show up here</p>
        <a href="/dashboard/student" class="start-ordering-btn">Start Ordering</a>
    </div>
    {{/if}}
</div>

<style>
    .orders-page {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
    }

    .page-header {
        background: white;
        padding: 24px;
        border-radius: 16px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
        margin-bottom: 24px;
        text-align: center;
    }

    .page-header h1 {
        font-size: 28px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 8px;
    }

    .page-header p {
        color: #4a5568;
        margin-bottom: 16px;
    }

    .back-btn {
        display: inline-block;
        background: #667eea;
        color: white;
        padding: 8px 16px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s ease;
    }

    .back-btn:hover {
        background: #5a67d8;
        transform: translateY(-1px);
    }

    .orders-list {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .order-card {
        background: white;
        border-radius: 16px;
        padding: 20px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
        border: 1px solid #e2e8f0;
    }

    .order-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 16px;
        padding-bottom: 16px;
        border-bottom: 1px solid #e2e8f0;
    }

    .order-info h3 {
        font-size: 18px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 4px;
    }

    .vendor-name {
        color: #667eea;
        font-weight: 500;
    }

    .order-status {
        text-align: right;
    }

    .status-badge {
        display: inline-block;
        padding: 4px 12px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        margin-bottom: 4px;
    }

    .status-PLACED {
        background: #fef3c7;
        color: #d97706;
    }

    .status-CONFIRMED {
        background: #dbeafe;
        color: #2563eb;
    }

    .status-PREPARING {
        background: #fed7d7;
        color: #dc2626;
    }

    .status-READY {
        background: #d1fae5;
        color: #059669;
    }

    .status-DELIVERED {
        background: #c6f6d5;
        color: #16a34a;
    }

    .status-CANCELLED {
        background: #f3f4f6;
        color: #6b7280;
    }

    .order-total {
        display: block;
        font-size: 18px;
        font-weight: 700;
        color: #1a202c;
    }

    .order-details {
        margin-bottom: 16px;
    }

    .order-items h4 {
        font-size: 14px;
        font-weight: 600;
        color: #4a5568;
        margin-bottom: 8px;
    }

    .order-item {
        display: flex;
        justify-content: space-between;
        padding: 6px 0;
        border-bottom: 1px solid #f7fafc;
    }

    .order-item:last-child {
        border-bottom: none;
    }

    .item-name {
        color: #1a202c;
        font-weight: 500;
    }

    .item-price {
        color: #667eea;
        font-weight: 600;
    }

    .order-meta {
        margin-top: 16px;
        padding-top: 16px;
        border-top: 1px solid #e2e8f0;
    }

    .order-meta p {
        margin-bottom: 4px;
        font-size: 14px;
        color: #4a5568;
    }

    .order-actions {
        display: flex;
        gap: 12px;
    }

    .track-btn,
    .reorder-btn {
        padding: 8px 16px;
        border-radius: 8px;
        font-weight: 500;
        text-decoration: none;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .track-btn {
        background: #667eea;
        color: white;
    }

    .track-btn:hover {
        background: #5a67d8;
    }

    .reorder-btn {
        background: #38a169;
        color: white;
    }

    .reorder-btn:hover {
        background: #2f855a;
    }

    .empty-orders {
        text-align: center;
        background: white;
        padding: 60px 40px;
        border-radius: 16px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
    }

    .empty-icon {
        font-size: 64px;
        margin-bottom: 16px;
    }

    .empty-orders h3 {
        font-size: 24px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 8px;
    }

    .empty-orders p {
        color: #4a5568;
        margin-bottom: 24px;
    }

    .start-ordering-btn {
        background: #667eea;
        color: white;
        padding: 12px 24px;
        border-radius: 12px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .start-ordering-btn:hover {
        background: #5a67d8;
        transform: translateY(-2px);
    }

    @media (max-width: 768px) {
        .orders-page {
            padding: 12px;
        }

        .order-header {
            flex-direction: column;
            gap: 12px;
            text-align: left;
        }

        .order-status {
            text-align: left;
        }

        .order-actions {
            flex-direction: column;
        }
    }
</style>

<script>
    function reorderItems(orderId) {
        if (confirm('Add all items from this order to your cart?')) {
            // This would typically make an API call to add items to cart
            alert('Items added to cart! (This is a demo)');
            // window.location.href = '/dashboard/student';
        }
    }
</script>