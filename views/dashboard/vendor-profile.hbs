<div class="vendor-profile-page">
    <div class="page-header">
        <h1>Business Profile</h1>
        <p>Manage your restaurant information and settings</p>
        <a href="/dashboard/vendor" class="back-btn">← Back to Dashboard</a>
    </div>

    {{#if messages.success}}
    <div class="alert alert-success">{{messages.success}}</div>
    {{/if}}

    {{#if messages.error}}
    <div class="alert alert-error">{{messages.error}}</div>
    {{/if}}

    <div class="profile-container">
        <div class="business-card">
            <div class="business-header">
                <div class="business-avatar">
                    <div class="avatar-circle">
                        {{vendor.name.[0]}}{{vendor.name.[1]}}
                    </div>
                    <div class="status-indicator {{#if vendor.isOnline}}online{{else}}offline{{/if}}"></div>
                </div>
                <div class="business-info">
                    <h2>{{vendor.name}}</h2>
                    <p class="cuisine-tag">{{vendor.cuisine}}</p>
                    <div class="rating-display">
                        <span class="rating">⭐ {{vendor.rating}}</span>
                        <span class="delivery-time">🕐 {{vendor.deliveryTime}}</span>
                    </div>
                </div>
            </div>

            <form action="/vendor/profile/update" method="POST" class="profile-form">
                <div class="form-section">
                    <h3>Business Information</h3>

                    <div class="form-group">
                        <label for="businessName">Business Name</label>
                        <input type="text" id="businessName" name="name" value="{{vendor.name}}" required>
                    </div>

                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" rows="3"
                            required>{{vendor.description}}</textarea>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label for="cuisine">Cuisine Type</label>
                            <select id="cuisine" name="cuisine" required>
                                <option value="Pizza" {{#if (eq vendor.cuisine 'Pizza' )}}selected{{/if}}>Pizza</option>
                                <option value="Coffee" {{#if (eq vendor.cuisine 'Coffee' )}}selected{{/if}}>Coffee &
                                    Beverages</option>
                                <option value="Indian" {{#if (eq vendor.cuisine 'Indian' )}}selected{{/if}}>Indian
                                </option>
                                <option value="Chinese" {{#if (eq vendor.cuisine 'Chinese' )}}selected{{/if}}>Chinese
                                </option>
                                <option value="Sandwiches" {{#if (eq vendor.cuisine 'Sandwiches' )}}selected{{/if}}>
                                    Sandwiches</option>
                                <option value="Fast Food" {{#if (eq vendor.cuisine 'Fast Food' )}}selected{{/if}}>Fast
                                    Food</option>
                                <option value="Desserts" {{#if (eq vendor.cuisine 'Desserts' )}}selected{{/if}}>Desserts
                                </option>
                                <option value="Healthy" {{#if (eq vendor.cuisine 'Healthy' )}}selected{{/if}}>Healthy
                                </option>
                                <option value="Stationary" {{#if (eq vendor.cuisine 'Stationary' )}}selected{{/if}}>
                                    Stationary</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="deliveryTime">Delivery Time</label>
                            <input type="text" id="deliveryTime" name="deliveryTime" value="{{vendor.deliveryTime}}"
                                placeholder="15-25 min">
                        </div>
                    </div>
                </div>

                <div class="form-section">
                    <h3>Contact Information</h3>

                    <div class="form-group">
                        <label for="ownerName">Owner Name</label>
                        <input type="text" id="ownerName" name="ownerName" value="{{user.name}}" required>
                    </div>

                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" value="{{user.email}}" disabled>
                        <small class="form-hint">Email cannot be changed</small>
                    </div>

                    <div class="form-group">
                        <label for="mobile">Mobile Number</label>
                        <input type="tel" id="mobile" name="mobile" value="{{user.mobile}}" pattern="[0-9]{10}"
                            required>
                    </div>

                    <div class="form-group">
                        <label for="address">Business Address</label>
                        <textarea id="address" name="address" rows="2" required>{{user.address}}</textarea>
                    </div>
                </div>

                <div class="form-section">
                    <h3>Business Settings</h3>

                    <div class="form-group">
                        <label class="checkbox-label">
                            <input type="checkbox" name="isActive" {{#if vendor.isActive}}checked{{/if}}>
                            <span>Business is active and accepting orders</span>
                        </label>
                    </div>

                    <div class="form-group">
                        <label class="checkbox-label">
                            <input type="checkbox" name="isOnline" {{#if vendor.isOnline}}checked{{/if}}>
                            <span>Currently online and available</span>
                        </label>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="save-btn">Save Changes</button>
                    <button type="button" class="cancel-btn" onclick="window.history.back()">Cancel</button>
                </div>
            </form>
        </div>

        <div class="sidebar">
            <div class="stats-card">
                <h3>Business Statistics</h3>
                <div class="stat-item">
                    <span class="stat-label">Total Orders</span>
                    <span class="stat-value">{{stats.totalOrders}}</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">Rating</span>
                    <span class="stat-value">{{vendor.rating}} ⭐</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">Revenue</span>
                    <span class="stat-value">₹{{formatCurrency stats.totalRevenue}}</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">Menu Items</span>
                    <span class="stat-value">{{stats.menuItemsCount}}</span>
                </div>
            </div>

            <div class="quick-actions-card">
                <h3>Quick Actions</h3>
                <a href="/dashboard/vendor/menu" class="action-link">
                    🍽️ Manage Menu
                </a>
                <a href="/dashboard/vendor/orders" class="action-link">
                    📋 View Orders
                </a>
                <a href="/dashboard/vendor/analytics" class="action-link">
                    📊 Analytics
                </a>
                <button class="action-link danger" onclick="showDeleteModal()">
                    🗑️ Delete Business
                </button>
            </div>

            <div class="hours-card">
                <h3>Business Hours</h3>
                <div class="hours-info">
                    <p>Monday - Friday: 9:00 AM - 10:00 PM</p>
                    <p>Saturday - Sunday: 10:00 AM - 11:00 PM</p>
                    <button class="edit-hours-btn" onclick="editHours()">Edit Hours</button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div id="deleteModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Delete Business</h3>
            <button onclick="closeDeleteModal()" class="close-modal">×</button>
        </div>
        <div class="modal-body">
            <p>Are you sure you want to delete your business? This action cannot be undone.</p>
            <p><strong>This will:</strong></p>
            <ul>
                <li>Remove all your menu items</li>
                <li>Cancel all pending orders</li>
                <li>Delete your business profile</li>
                <li>Remove all customer reviews</li>
            </ul>
            <p>Type <strong>DELETE</strong> to confirm:</p>
            <input type="text" id="deleteConfirm" placeholder="Type DELETE">
        </div>
        <div class="modal-actions">
            <button onclick="closeDeleteModal()" class="cancel-btn">Cancel</button>
            <button onclick="confirmDelete()" class="delete-btn" id="confirmDeleteBtn" disabled>Delete Business</button>
        </div>
    </div>
</div>

<style>
    .vendor-profile-page {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
    }

    .page-header {
        background: white;
        padding: 24px;
        border-radius: 16px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
        margin-bottom: 24px;
        text-align: center;
    }

    .page-header h1 {
        font-size: 28px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 8px;
    }

    .page-header p {
        color: #4a5568;
        margin-bottom: 16px;
    }

    .back-btn {
        display: inline-block;
        background: #667eea;
        color: white;
        padding: 8px 16px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s ease;
    }

    .back-btn:hover {
        background: #5a67d8;
        transform: translateY(-1px);
    }

    .alert {
        padding: 12px 16px;
        border-radius: 8px;
        margin-bottom: 20px;
        font-size: 14px;
    }

    .alert-success {
        background: #f0fff4;
        color: #38a169;
        border: 1px solid #c6f6d5;
    }

    .alert-error {
        background: #fee;
        color: #c53030;
        border: 1px solid #fed7d7;
    }

    .profile-container {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 24px;
    }

    .business-card {
        background: white;
        border-radius: 16px;
        padding: 24px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
    }

    .business-header {
        display: flex;
        align-items: center;
        gap: 20px;
        margin-bottom: 32px;
        padding-bottom: 24px;
        border-bottom: 1px solid #e2e8f0;
    }

    .business-avatar {
        position: relative;
    }

    .avatar-circle {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        font-weight: 700;
    }

    .status-indicator {
        position: absolute;
        bottom: 5px;
        right: 5px;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        border: 3px solid white;
    }

    .status-indicator.online {
        background: #10b981;
    }

    .status-indicator.offline {
        background: #ef4444;
    }

    .business-info h2 {
        font-size: 24px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 4px;
    }

    .cuisine-tag {
        background: #667eea;
        color: white;
        padding: 2px 8px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
        display: inline-block;
        margin-bottom: 8px;
    }

    .rating-display {
        display: flex;
        gap: 16px;
        font-size: 14px;
        color: #4a5568;
    }

    .form-section {
        margin-bottom: 32px;
    }

    .form-section h3 {
        font-size: 18px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 16px;
        padding-bottom: 8px;
        border-bottom: 1px solid #e2e8f0;
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
    }

    .form-group label {
        display: block;
        margin-bottom: 6px;
        font-weight: 600;
        color: #4a5568;
        font-size: 14px;
    }

    .form-group input,
    .form-group textarea,
    .form-group select {
        width: 100%;
        padding: 12px 16px;
        border: 2px solid #e2e8f0;
        border-radius: 8px;
        font-size: 16px;
        transition: all 0.3s ease;
        box-sizing: border-box;
        font-family: inherit;
    }

    .form-group input:focus,
    .form-group textarea:focus,
    .form-group select:focus {
        outline: none;
        border-color: #667eea;
        box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    .form-group input:disabled {
        background: #f7fafc;
        color: #718096;
        cursor: not-allowed;
    }

    .form-hint {
        display: block;
        color: #718096;
        font-size: 12px;
        margin-top: 4px;
    }

    .checkbox-label {
        display: flex;
        align-items: center;
        gap: 8px;
        cursor: pointer;
        font-size: 14px;
    }

    .checkbox-label input[type="checkbox"] {
        width: auto;
        margin: 0;
    }

    .form-actions {
        display: flex;
        gap: 12px;
        padding-top: 24px;
        border-top: 1px solid #e2e8f0;
    }

    .save-btn,
    .cancel-btn {
        padding: 12px 24px;
        border-radius: 8px;
        font-weight: 600;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .save-btn {
        background: #10b981;
        color: white;
        flex: 1;
    }

    .save-btn:hover {
        background: #059669;
    }

    .cancel-btn {
        background: #e2e8f0;
        color: #4a5568;
    }

    .cancel-btn:hover {
        background: #cbd5e0;
    }

    .sidebar {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .stats-card,
    .quick-actions-card,
    .hours-card {
        background: white;
        border-radius: 16px;
        padding: 20px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
    }

    .stats-card h3,
    .quick-actions-card h3,
    .hours-card h3 {
        font-size: 16px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 16px;
    }

    .stat-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 8px 0;
        border-bottom: 1px solid #f7fafc;
    }

    .stat-item:last-child {
        border-bottom: none;
    }

    .stat-label {
        color: #4a5568;
        font-size: 14px;
    }

    .stat-value {
        font-weight: 600;
        color: #1a202c;
    }

    .action-link {
        display: block;
        padding: 12px 16px;
        margin-bottom: 8px;
        background: #f7fafc;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        text-decoration: none;
        color: #4a5568;
        font-weight: 500;
        transition: all 0.3s ease;
        cursor: pointer;
        text-align: left;
        width: 100%;
        box-sizing: border-box;
    }

    .action-link:hover {
        background: #e2e8f0;
        transform: translateY(-1px);
    }

    .action-link:last-child {
        margin-bottom: 0;
    }

    .action-link.danger {
        color: #e53e3e;
        border-color: #feb2b2;
    }

    .action-link.danger:hover {
        background: #fed7d7;
        border-color: #fc8181;
    }

    .hours-info p {
        margin-bottom: 8px;
        color: #4a5568;
        font-size: 14px;
    }

    .edit-hours-btn {
        background: #667eea;
        color: white;
        padding: 8px 16px;
        border: none;
        border-radius: 6px;
        font-size: 12px;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .edit-hours-btn:hover {
        background: #5a67d8;
    }

    .modal {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
        z-index: 2000;
        align-items: center;
        justify-content: center;
    }

    .modal-content {
        background: white;
        border-radius: 20px;
        max-width: 500px;
        width: 90%;
        max-height: 90vh;
        overflow-y: auto;
    }

    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 24px 24px 0 24px;
        margin-bottom: 20px;
    }

    .modal-header h3 {
        margin: 0;
        font-size: 20px;
        font-weight: 700;
        color: #e53e3e;
    }

    .close-modal {
        background: none;
        border: none;
        font-size: 24px;
        cursor: pointer;
        color: #718096;
        padding: 4px;
    }

    .modal-body {
        padding: 0 24px;
        margin-bottom: 20px;
    }

    .modal-body p {
        margin-bottom: 12px;
        color: #4a5568;
    }

    .modal-body ul {
        margin: 12px 0;
        padding-left: 20px;
        color: #4a5568;
    }

    .modal-body input {
        width: 100%;
        padding: 12px 16px;
        border: 2px solid #e2e8f0;
        border-radius: 8px;
        font-size: 16px;
        margin-top: 8px;
        box-sizing: border-box;
    }

    .modal-actions {
        display: flex;
        gap: 12px;
        padding: 0 24px 24px 24px;
    }

    .delete-btn {
        background: #e53e3e;
        color: white;
        padding: 12px 24px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        flex: 1;
    }

    .delete-btn:hover:not(:disabled) {
        background: #dc2626;
    }

    .delete-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
    }

    @media (max-width: 768px) {
        .vendor-profile-page {
            padding: 12px;
        }

        .profile-container {
            grid-template-columns: 1fr;
            gap: 16px;
        }

        .business-header {
            flex-direction: column;
            text-align: center;
            gap: 12px;
        }

        .form-row {
            grid-template-columns: 1fr;
        }

        .form-actions {
            flex-direction: column;
        }
    }
</style>

<script>
    function showDeleteModal() {
        document.getElementById('deleteModal').style.display = 'flex';
        document.body.style.overflow = 'hidden';
    }

    function closeDeleteModal() {
        document.getElementById('deleteModal').style.display = 'none';
        document.body.style.overflow = 'auto';
        document.getElementById('deleteConfirm').value = '';
        document.getElementById('confirmDeleteBtn').disabled = true;
    }

    function confirmDelete() {
        const confirmText = document.getElementById('deleteConfirm').value;
        if (confirmText === 'DELETE') {
            if (confirm('This action cannot be undone. Are you absolutely sure?')) {
                // In a real app, this would make an API call to delete the business
                alert('Business deletion would be processed here');
                closeDeleteModal();
            }
        }
    }

    function editHours() {
        alert('Business hours editing functionality would be implemented here');
    }

    // Enable/disable delete button based on confirmation text
    document.getElementById('deleteConfirm').addEventListener('input', function () {
        const deleteBtn = document.getElementById('confirmDeleteBtn');
        deleteBtn.disabled = this.value !== 'DELETE';
    });

    // Auto-format mobile number
    document.getElementById('mobile').addEventListener('input', function (e) {
        this.value = this.value.replace(/\D/g, '').slice(0, 10);
    });

    // Close modal when clicking outside
    document.getElementById('deleteModal').addEventListener('click', function (e) {
        if (e.target === this) {
            closeDeleteModal();
        }
    });
</script>